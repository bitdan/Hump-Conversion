user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
    multi_accept on;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # 日志格式定义
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    # 基础优化配置
    sendfile            on;
    tcp_nopush         on;
    tcp_nodelay        on;
    keepalive_timeout  65;
    types_hash_max_size 2048;

    # GZIP 压缩（保留，对带宽有帮助）
    gzip on;
    gzip_disable "msie6";
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_buffers 16 8k;
    gzip_http_version 1.1;
    gzip_min_length 256;
    gzip_types
        application/atom+xml
        application/geo+json
        application/javascript
        application/x-javascript
        application/json
        application/ld+json
        application/manifest+json
        application/rdf+xml
        application/rss+xml
        application/xml
        application/vnd.ms-fontobject
        application/wasm
        application/x-web-app-manifest+json
        application/xhtml+xml
        application/x-font-ttf
        application/x-font-opentype
        application/x-font-truetype
        application/x-www-form-urlencoded
        font/eot
        font/opentype
        font/otf
        image/bmp
        image/svg+xml
        image/x-icon
        text/cache-manifest
        text/calendar
        text/css
        text/javascript
        text/plain
        text/markdown
        text/vcard
        text/vnd.rim.location.xloc
        text/vtt
        text/x-component
        text/x-cross-domain-policy;

    # 安全相关配置
    server_tokens off;
    add_header X-Frame-Options SAMEORIGIN;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    client_max_body_size 10M;

    # 🔥 默认服务器配置 - 修改为不缓存
    server {
        listen       80 default_server;
        listen       [::]:80 default_server;
        server_name  _;

        root /usr/share/nginx/html;
        index index.html index.htm;

        # 🔥 移除静态资源缓存配置，改为全局不缓存
        location / {
            try_files $uri $uri/ =404;

            # 🔥 强制不缓存所有资源
            add_header Cache-Control "no-cache, no-store, must-revalidate";
            add_header Pragma "no-cache";
            expires 0;
        }

        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root /usr/share/nginx/html;

            # 🔥 错误页面也不缓存
            add_header Cache-Control "no-cache, no-store, must-revalidate";
            add_header Pragma "no-cache";
            expires 0;
        }
    }

    # 🔥 工具网站配置 - 修改为不缓存
    server {
        listen       80;
        server_name  tool.linger.host;

        root /usr/share/nginx/html/tool;
        index index.html index.htm;

        # 🔥 移除原有的静态资源缓存配置
        # 原来的配置注释掉或删除：
        # location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
        #     expires 1d;
        #     add_header Cache-Control "public, no-transform";
        # }

        location /api/ {
            proxy_pass http://43.156.83.246:8000;
            proxy_http_version 1.1;

            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_read_timeout 86400s;
            proxy_send_timeout 86400s;
            proxy_buffering off;
            proxy_cache off;
        }

        # 首页和已注册的前端路由进入 SPA。未知路径不再统一回退到 index.html。
        location = / {
            try_files /index.html =404;

            add_header Cache-Control "no-cache, no-store, must-revalidate";
            add_header Pragma "no-cache";
            expires 0;
        }

        location = /index.html {
            return 301 /;
        }

        location ~ ^/tools(?:/(?:skill-chat-workspace|langgraph-viewer|mysql-analysis|mcp-tester|string-generator|case-converter|file-diff|epub-reader|json-editor|calculator|timestamp|cron-helper|data-encryption|qr-code-generator|code-shot-generator|pixi-lab|two-factor-manager|profile-center|admin-users|jetbrain-checker|markdown-resume-pdf))?/?$ {
            try_files /index.html =404;

            add_header Cache-Control "no-cache, no-store, must-revalidate";
            add_header Pragma "no-cache";
            expires 0;
        }

        location ~ ^/market/review/?$ {
            try_files /index.html =404;

            add_header Cache-Control "no-cache, no-store, must-revalidate";
            add_header Pragma "no-cache";
            expires 0;
        }

        location ~ ^/community/posts(?:/new|/[^/]+(?:/edit)?)?/?$ {
            try_files /index.html =404;

            add_header Cache-Control "no-cache, no-store, must-revalidate";
            add_header Pragma "no-cache";
            expires 0;
        }

        location ~ ^/games/(?:dice-roller|sudoku|tic-tac-toe|puzzle|gomoku|go|chinese-chess|blind-chinese-chess|snake|tetris|2048|pacman|link-game|minesweeper|breakout|angry-birds|bubble-shooter|zuma)/?$ {
            try_files /index.html =404;

            add_header Cache-Control "no-cache, no-store, must-revalidate";
            add_header Pragma "no-cache";
            expires 0;
        }

        location ~ ^/auth/(?:login|register)/?$ {
            try_files /index.html =404;

            add_header Cache-Control "no-cache, no-store, must-revalidate";
            add_header Pragma "no-cache";
            expires 0;
        }

        location / {
            try_files $uri $uri/ =404;

            # 🔥 强制不缓存所有资源（覆盖原有配置）
            add_header Cache-Control "no-cache, no-store, must-revalidate";
            add_header Pragma "no-cache";
            expires 0;
        }

        error_page 404 /404.html;
        location = /404.html {
            internal;
        }

        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root /usr/share/nginx/html;

            # 🔥 错误页面也不缓存
            add_header Cache-Control "no-cache, no-store, must-revalidate";
            add_header Pragma "no-cache";
            expires 0;
        }
    }
}

